@namespace("com.databricks.parquet.avro")
protocol ParquetAvroCompat {
  record AvroIntArray {
    array<int> f;
  }

  record AvroArrayOfOptionalInt {
    array<union { null, int }> f;
  }

  record AvroArrayOfIntArray {
    array<array<int>> f;
  }
}
