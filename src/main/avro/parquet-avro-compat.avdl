@namespace("com.databricks.avro")
protocol ParquetAvroCompat {
  record AvroIntArray {
    array<int> f;
  }

  record AvroNullableIntArray {
    array<union { null, int }> f;
  }

  record AvroArrayOfIntArray {
    array<array<int>> f;
  }
}
